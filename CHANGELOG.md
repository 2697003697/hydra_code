# Changelog

所有重要变更将在此文件中记录。

## [0.4.1] - 2026-02-22

### 优化

#### 1. Auto 模式智能路由增强
- **多维度分析**: 路由分析现在会提取 `complexity`（复杂度）、`domain`（领域）、`intent`（意图）三个维度，而非仅判断简单/复杂。
- **领域感知**: Leader 模式会根据任务领域（coding/content/general）调整策略：
  - `coding`: 严格遵守代码规范，确保代码可运行
  - `content`: 侧重于结构清晰、语言优美
  - `general`: 直接回答问题，必要时调用工具验证
- **意图感知**: Leader 模式会根据用户意图调整行为：
  - `new`: 从头设计实现，确保架构合理
  - `modify`: 先理解现有代码，只修改必要部分
  - `qa`: 提供准确详尽的解答
- **复杂度分级**: 区分 `simple`/`moderate`/`complex` 三级复杂度，Leader 会根据复杂度调整计划详细程度。

#### 2. 路由分析容错增强
- **Fallback 机制**: 当 Fast 模型分析失败时，自动回退到 Pro 模型进行路由判断。
- **模型选择优化**: 简单任务优先使用 Fast 模型（速度优先），复杂任务使用 Leader 模式统筹。

### 修复

#### 1. force_mode 处理逻辑修复
- **现象**: `/parallel` 命令设置的 `force_mode="complex"` 未被正确处理，导致与 `/auto` 行为相同。
- **修复**: 将 `force_mode="complex"` 改为 `force_mode="parallel"`，确保并行模式正确执行。

#### 2. 死代码清理
- **清理**: 移除了 `coordinator.py` 中未使用的 `_full_workflow`、`_create_plan`、`_execute_task_with_collaboration` 等方法。

## [0.4.0] - 2026-02-22

### 新增特性

#### 1. Leader-Worker 协作架构
- **功能**: 引入全新的 Leader-Worker 协作模式，由指定的 Leader 模型（默认为 Opus）负责统筹规划、任务分发、结果审查和最终整合。
- **命令**: 
  - `/leader [role]`: 切换到 Leader 模式。可指定 Leader 角色（如 `/leader sonnet`），默认为 `opus`。
  - `/parallel`: 原 `/complex` 模式更名为 `/parallel`，用于无 Leader 的并行协作。
- **智能路由**: `/auto` 模式现在会将复杂任务自动路由到 Leader 模式，而非旧版的并行模式，提供更强的任务掌控力。
- **体验**: Leader 模式输出风格与其他模式统一，支持流式输出和思考过程折叠。

### 优化

#### 1. 命令行交互优化
- **重命名**: `/complex` 命令重命名为 `/parallel`，语义更清晰。
- **帮助信息**: 更新 `/help` 内容，包含新的协作模式说明。

### 修复

#### 1. 路由分析错误修复
- **现象**: 在某些情况下，路由分析可能因模型返回空内容而报错 "expected string or bytes-like object, got 'NoneType'"。
- **修复**: 增强了 `coordinator.py` 中的 `_call_agent` 方法，确保始终返回字符串，避免 NoneType 错误。

## [0.3.2] - 2026-02-22

### 新增特性

#### 1. 多模型库管理与动态切换
- **功能**: 支持在配置文件中定义 `models` 列表，存储多个模型配置（包含 `description`, `base_url`, `provider` 等）。
- **配置**: `roles` 可以直接引用 `models` 中定义的模型名称，实现配置复用。
- **CLI**: 新增 `/model` 命令：
  - `/model list`: 列出所有可用模型及其详情（包括描述和API地址）。
  - `/model use <role> <model_name>`: 运行时动态切换角色的底层模型。

#### 2. 思考窗口 (Thinking Window) UI 优化
- **优化**: 解决了思考过程过长导致 UI 无法滚动和内容阻塞的问题。
- **实现**: 
  - 引入滑动窗口机制，仅展示最近 20 行思考内容，保持界面流畅。
  - 思考结束后自动折叠为摘要行，释放屏幕空间。
  - 移除了低效的字符串拼接逻辑，提升渲染性能。

#### 3. 配置系统增强
- **功能**: 支持为每个模型单独配置 `max_tokens`。
- **功能**: 支持 `description` 字段，用于区分同名但不同平台的模型（如 "Azure版" vs "官方版"）。
- **修复**: 修复了 YAML 解析错误和配置保存逻辑，确保模型引用关系正确持久化。

## [0.3.1] - 2026-02-20

### 修复

#### 1. config.py role_configs 问题修复
- **现象**: 当配置文件中没有定义 roles 时，向 Config 构造函数传递 None 而非空字典
- **原因**: parse_config 函数中使用了 `role_configs if role_configs else None` 表达式
- **修复**: 简化为直接传递 `role_configs`，Config.__post_init__ 会正确处理空字典情况

#### 2. parallel.py 重复导入清理
- **现象**: 文件中存在重复的 `from rich.live import Live` 导入
- **修复**: 删除了重复的导入语句，保持代码整洁
#### 3. config.py 示例中单个模型未添加max_tokens 问题修复
- **现象**: 示例配置文件中单个模型未添加 max_tokens 字段，用户可能无法意识到能够设置该参数
- **修复**: 在示例配置文件中为单个模型添加 max_tokens 字段，默认值设为 4096

## [0.3.0] - 2026-02-20

### 修复

#### 1. 消息顺序问题修复
- **现象**: `get_context_for_model` 获取的消息顺序混乱
- **原因**: 使用 `reversed()` 遍历消息时，插入逻辑有误
- **修复**: 重构遍历逻辑，确保消息按时间正序排列（最旧的消息在前，最新的在后），符合模型上下文要求

#### 2. 压缩判断逻辑问题修复
- **现象**: 即使 Token 数超标，如果消息条数未超标，也不会触发压缩
- **原因**: 判断条件逻辑错误，需同时满足两个条件才压缩
- **修复**: 将判断逻辑改为"消息数量超标 **或** Token 数量超标"即触发压缩

#### 3. 重要性覆盖问题修复
- **现象**: 用户手动指定的消息 `importance` 参数被系统默认值覆盖
- **修复**: 添加条件判断，仅当 `importance` 为 0（默认值）时才应用系统自动评分逻辑，保留用户手动设置的高优先级

#### 4. 正则提取 Bug 修复
- **现象**: `_extract_key_info` 无法正确提取文件名，例如输入"我创建了文件 test.py"，系统错误地提取为 ['创建了文']
- **原因**: 正则表达式 `r'创建[了]?\s*[文件|项目|模块]'` 缺少对文件名的捕获组
- **修复**: 更新了 `create_patterns` 和 `modify_patterns`，增加了对文件名的捕获组（如 `([a-zA-Z0-9_\-\./\\]+\.[a-zA-Z0-9]+)`），现在能正确提取文件名

#### 5. 工具调用原子性保护
- **现象**: 当对话历史过长触发 `max_tokens` 限制时，截断点可能发生在 Assistant 调用工具和工具返回结果之间，导致工具返回结果保留但调用消息被丢弃，造成发送给模型的上下文以 tool 消息开头，违反 LLM API 协议，引发 422 Invalid tool protocol 错误
- **修复**: 在 `get_context_for_model` 方法中加入原子性保护逻辑：
  - 软限制保护: 遍历消息时，如果即将因为 Token 超限停止，且当前上下文第一条消息是 tool 类型，系统会检查即将丢弃的消息是否是发起该调用的 assistant 消息，如是则强制包含
  - 安全清理: 在构建最终上下文前，移除以 tool 消息开头的孤儿工具消息

## [0.2.0] - 之前版本

### 特性
- 初始版本发布
